const EPSILON: f64 = 1.0; // Depth of the potential well
const SIGMA: f64 = 1.0; // Finite distance at which the inter-particle potential is zero
const MASS: f64 = 1.0; // Mass of each particle
const DT: f64 = 0.01; // Time step
const NUM_STEPS: usize = 10000; // Number of simulation steps
